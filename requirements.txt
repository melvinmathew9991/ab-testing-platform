# Runtime dependencies, pinned for reproducible builds.
# The floors that define compatibility live in pyproject.toml; these are the
# exact versions this project is tested and deployed with.
Jinja2==3.1.6
matplotlib==3.10.9
numpy==2.2.6
pandas==2.3.3
PyYAML==6.0.3
scipy==1.15.3
